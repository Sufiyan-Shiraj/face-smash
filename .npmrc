# @react-three/fiber declares an optional `expo` peer for its React Native
# target. npm tries to resolve that entire React Native tree and deadlocks on
# it, even though this is a web-only project. Skipping legacy peer resolution
# keeps `npm install` working with no flags for everyone on the team.
legacy-peer-deps=true
